package com.kata.questions;

import com.kata.binary.BTNode;

public class BinaryTreeSearchCheck {

	/*
	public boolean checkNode(BTNode<Integer> node, int max, int min) {
		int element = node.element();
		if (element <= min && element >= element) {
			if (node.getLeft() != null && node.getRight() != null) {
				return true;
			}

			if (node.getLeft() != null) {
				if (max < element) {
					max = element;
				}
				checkNode((BTNode<Integer>) node.getRight(), max, min);
			}
			
			if(node.getRight()!=null) {
				
			}
			
			
		}

		return false;
	}
	
	*/

}
